name: Deploy public web to GitHub Pages

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deferred:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      (vars.SANDFEST_TURNSTILE_SITE_KEY == '' || vars.SANDFEST_APPLE_APP_ID_PREFIX == '')
    runs-on: ubuntu-latest
    steps:
      - name: Record deferred production release
        run: echo "::notice::Public release is deferred until the post-board production Turnstile site key and Apple application identifier prefix are configured. No artifact was built or deployed."

  build:
    if: >-
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'main' &&
      vars.SANDFEST_TURNSTILE_SITE_KEY != '' &&
      vars.SANDFEST_APPLE_APP_ID_PREFIX != ''
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Require production Turnstile registration
        env:
          VITE_SANDFEST_TURNSTILE_SITE_KEY: ${{ vars.SANDFEST_TURNSTILE_SITE_KEY }}
        run: test -n "$VITE_SANDFEST_TURNSTILE_SITE_KEY" || (echo "::error::Set repository variable SANDFEST_TURNSTILE_SITE_KEY to the production Cloudflare widget site key." && exit 1)
      - name: Require Apple application identifier prefix
        env:
          SANDFEST_APPLE_APP_ID_PREFIX: ${{ vars.SANDFEST_APPLE_APP_ID_PREFIX }}
        run: test -n "$SANDFEST_APPLE_APP_ID_PREFIX" || (echo "::error::Set repository variable SANDFEST_APPLE_APP_ID_PREFIX to the signed app's 10-character application identifier prefix." && exit 1)
      - name: Prove production API is launch-ready
        env:
          SANDFEST_APPLE_APP_ID_PREFIX: ${{ vars.SANDFEST_APPLE_APP_ID_PREFIX }}
          SANDFEST_LIVE_PUBLIC_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
          SANDFEST_LIVE_API_URL: https://sandfest-api.heyelab.com/
          SANDFEST_LIVE_ADMIN_URL: https://sandfest-admin.heyelab.com/
        run: npm run deployment:verify:api
      - name: Build visitor-only static site
        env:
          # Pages publishes at https://<owner>.github.io/<repo>/
          DEPLOY_BASE: /${{ github.event.repository.name }}/
          SANDFEST_DEPLOYMENT_ENV: production
          SANDFEST_APPLE_APP_ID_PREFIX: ${{ vars.SANDFEST_APPLE_APP_ID_PREFIX }}
          # Public identifier from the production Turnstile widget. The Vite
          # build rejects missing and Cloudflare test keys.
          VITE_SANDFEST_TURNSTILE_SITE_KEY: ${{ vars.SANDFEST_TURNSTILE_SITE_KEY }}
        run: npm run build:public && npm run test:pages-artifact
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist-public

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: "npm"
      - run: npm ci
      - name: Re-prove production API immediately before deploy
        env:
          SANDFEST_APPLE_APP_ID_PREFIX: ${{ vars.SANDFEST_APPLE_APP_ID_PREFIX }}
          SANDFEST_LIVE_PUBLIC_URL: https://${{ github.repository_owner }}.github.io/${{ github.event.repository.name }}/
          SANDFEST_LIVE_API_URL: https://sandfest-api.heyelab.com/
          SANDFEST_LIVE_ADMIN_URL: https://sandfest-admin.heyelab.com/
        run: npm run deployment:verify:api
      - id: deployment
        uses: actions/deploy-pages@v4
